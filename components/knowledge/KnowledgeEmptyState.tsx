import Link from "next/link";
export default function KnowledgeEmptyState({ title, detail, action, href, onAction }: { title: string; detail: string; action: string; href?: string; onAction?: () => void }) { const classes="mt-5 inline-flex rounded-xl bg-cyan-300 px-4 py-2.5 text-sm font-semibold text-slate-950 outline-none focus-visible:ring-2 focus-visible:ring-white"; return <div className="rounded-3xl border border-dashed border-white/15 bg-white/[0.025] p-7 text-center"><h3 className="font-medium text-slate-100">{title}</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-400">{detail}</p>{href ? <Link href={href} className={classes}>{action}</Link> : <button type="button" onClick={onAction} className={classes}>{action}</button>}</div>; }
