import { Telescope } from "lucide-react";
export type OutlookItem = { id: string; trust: "Expected"|"Estimated"|"Recommended"; text: string };
export default function ExecutiveOutlook({ items }: { items: OutlookItem[] }) { return <section aria-labelledby="outlook" className="rounded-3xl bg-white/[0.045] p-6"><div className="flex items-center gap-3"><Telescope className="text-cyan-300" size={20} /><div><p className="text-sm text-cyan-300">What is likely next</p><h2 id="outlook" className="mt-1 text-2xl font-semibold">Outlook</h2></div></div>{items.length ? <ul className="mt-5 space-y-3">{items.slice(0,3).map((item)=><li key={item.id} className="rounded-2xl bg-slate-950/25 p-4"><span className="text-[11px] text-slate-500">{item.trust}</span><p className="mt-1 text-sm leading-5 text-slate-300">{item.text}</p></li>)}</ul> : <p className="mt-5 text-sm text-slate-400">Not enough recent activity to generate a reliable outlook.</p>}</section>; }
