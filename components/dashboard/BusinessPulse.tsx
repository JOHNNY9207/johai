import Link from "next/link";
import { ArrowRight, TrendingDown, TrendingUp } from "lucide-react";
export default function BusinessPulse({ status, explanation, trend, positive, constraint, confidence, updatedAt }: { status: string; explanation: string; trend: "up" | "down" | "steady"; positive: string; constraint: string; confidence: number; updatedAt: string }) {
  const TrendIcon = trend === "down" ? TrendingDown : TrendingUp;
  return <section aria-labelledby="business-pulse" className="rounded-3xl bg-white/[0.045] p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-cyan-300">Business pulse</p><h2 id="business-pulse" className="mt-1 text-2xl font-semibold">{status}</h2></div><span className="flex items-center gap-1.5 text-xs text-slate-400"><TrendIcon size={15} aria-hidden="true" />{trend === "steady" ? "No historical trend" : trend === "up" ? "Positive movement" : "Needs attention"}</span></div><p className="mt-4 text-sm leading-6 text-slate-300">{explanation}</p><dl className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-slate-950/30 p-4"><dt className="text-xs text-emerald-300">Main positive signal</dt><dd className="mt-2 text-sm text-slate-200">{positive}</dd></div><div className="rounded-2xl bg-slate-950/30 p-4"><dt className="text-xs text-amber-300">Main constraint</dt><dd className="mt-2 text-sm text-slate-200">{constraint}</dd></div></dl><div className="mt-5 flex flex-wrap items-center justify-between gap-3 text-xs text-slate-500"><p>{confidence}% data completeness · Updated {updatedAt}</p><div className="flex gap-4"><Link href="#ai-brain" className="text-cyan-200 outline-none focus-visible:ring-2 focus-visible:ring-cyan-300">Open Business Brain <ArrowRight className="inline" size={13} /></Link><Link href="#analytics" className="text-cyan-200 outline-none focus-visible:ring-2 focus-visible:ring-cyan-300">View details</Link></div></div></section>;
}
