import { Bot, CheckCircle2, Clock3 } from "lucide-react";
import EvidenceTimeline, { type EvidenceItem } from "./EvidenceTimeline";
export default function AIWorkReport({ summary, current, stage, progress, expected, next, approval, evidence }: { summary: string; current: string; stage: string; progress: number; expected: string; next: string; approval: boolean; evidence: EvidenceItem[] }) { return <section aria-labelledby="ai-work-report" className="rounded-3xl bg-white/[0.045] p-6"><div className="flex items-center gap-3"><Bot className="text-cyan-300" size={21} aria-hidden="true" /><div><p className="text-sm text-cyan-300">Completed by JOHAI</p><h2 id="ai-work-report" className="mt-1 text-2xl font-semibold">JOHAI at work</h2></div></div><p className="mt-5 text-base leading-7 text-slate-200">{summary}</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-slate-950/30 p-5"><p className="text-xs text-slate-500">Current task · {stage}</p><h3 className="mt-2 font-medium text-slate-100">{current}</h3><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-white/10" role="progressbar" aria-label="Current task progress" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100}><div className="h-full rounded-full bg-cyan-300" style={{width:`${progress}%`}} /></div><p className="mt-3 text-xs text-slate-400">Expected result: {expected}</p></div><div className="rounded-2xl bg-slate-950/30 p-5"><p className="flex items-center gap-2 text-xs text-slate-500"><Clock3 size={14} />Next planned action</p><h3 className="mt-2 font-medium text-slate-100">{next}</h3><p className="mt-3 flex items-center gap-2 text-xs text-slate-400"><CheckCircle2 size={14} className={approval ? "text-amber-300" : "text-emerald-300"} />{approval ? "Waiting for approval" : "No approval required"}</p></div></div><EvidenceTimeline items={evidence} /></section>; }
