import { Check } from "lucide-react";
export type EvidenceItem = { id: string; label: string; detail: string };
export default function EvidenceTimeline({ items }: { items: EvidenceItem[] }) { return <details className="mt-5 rounded-2xl bg-slate-950/25 p-4"><summary className="cursor-pointer text-sm font-medium text-slate-300 outline-none focus-visible:ring-2 focus-visible:ring-cyan-300">View recorded evidence</summary>{items.length ? <ol className="mt-4 space-y-3">{items.slice(0,6).map((item) => <li key={item.id} className="flex gap-3 text-sm"><Check className="mt-0.5 shrink-0 text-emerald-300" size={15} aria-hidden="true" /><div><p className="text-slate-200">{item.label}</p><p className="mt-0.5 text-xs text-slate-500">{item.detail}</p></div></li>)}</ol> : <p className="mt-4 text-sm text-slate-500">No recent activity has been recorded.</p>}</details>; }
