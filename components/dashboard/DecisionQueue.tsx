import Link from "next/link";
import { ArrowRight, CheckCircle2 } from "lucide-react";
export type ExecutiveDecision = { id: string; title: string; why: string; urgency: string; action: string; href: string; after: string };
export default function DecisionQueue({ items }: { items: ExecutiveDecision[] }) { return <section aria-labelledby="decision-queue" className={`rounded-3xl p-6 ${items.length ? "bg-amber-300/[0.07]" : "bg-white/[0.035]"}`}><div className="flex items-center justify-between gap-3"><div><p className="text-sm text-amber-300">Human decisions</p><h2 id="decision-queue" className="mt-1 text-2xl font-semibold">Decision queue</h2></div>{items.length > 0 && <span className="rounded-full bg-amber-300/15 px-2.5 py-1 text-xs text-amber-100">{items.length} waiting</span>}</div>{items.length === 0 ? <div className="mt-5 flex gap-3 text-sm text-slate-300"><CheckCircle2 className="shrink-0 text-emerald-300" size={18} aria-hidden="true" /><p>No decisions required. JOHAI is handling current work.</p></div> : <ol className="mt-5 space-y-4">{items.slice(0,3).map((item) => <li key={item.id} className="border-t border-white/10 pt-4 first:border-0 first:pt-0"><div className="flex items-start justify-between gap-3"><h3 className="font-medium text-slate-100">{item.title}</h3><span className="shrink-0 text-[11px] text-amber-200">{item.urgency}</span></div><p className="mt-2 text-sm leading-5 text-slate-400">{item.why}</p><p className="mt-2 text-xs text-slate-500">Afterward: {item.after}</p><Link href={item.href} className="mt-3 inline-flex items-center gap-1.5 rounded-xl bg-amber-200 px-3 py-2 text-xs font-semibold text-slate-950 outline-none focus-visible:ring-2 focus-visible:ring-white">{item.action}<ArrowRight size={13} /></Link></li>)}</ol>}</section>; }
